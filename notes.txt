React Frontend
- User uploads image
- Shows image preview
- Sends image to backend
- Displays prediction and confidence

        ↓

Python Backend
- Receives image
- Preprocesses image
- Loads final_model.keras
- Runs prediction
- Returns result as JSON

        ↓

Saved Model
- final_model.keras
- class_names.json

flask = python framework which will be used to create web servers and APIs

need to use flask since react wont be able to load and run the tf Model
